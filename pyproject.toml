[project]
name = "autox-tools"
version = "0.1.0"
description = "Internal scripts and tooling for evaluating and operating on projects."
requires-python = ">=3.11,<3.14"
dependencies = [
    "boto3>=1.28",
    "kfp>=2.15.2",
    "kubernetes>=28.0",
    "ogx-client>=1.0.2",
    "openai>=1.30",
    "psycopg[binary]>=3.1",
    "pymilvus>=2.4",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "tabulate>=0.9",
]

[project.optional-dependencies]
reports = ["matplotlib>=3.8"]

[project.scripts]
vs = "autox_tools.vs.cli:main"
pipelines = "autox_tools.pipelines.cli:main"
s3 = "autox_tools.s3.cli:main"
autorag = "autox_tools.autorag.cli:main"
automl = "autox_tools.automl.cli:main"
ogx = "autox_tools.ogx.cli:main"
maas = "autox_tools.maas.cli:main"
secrets = "autox_tools.secrets.cli:main"
config = "autox_tools.config.cli:main"

[dependency-groups]
dev = [
    "mypy>=1.10",
    "pytest>=8.0",
    "ruff>=0.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "SIM", # flake8-simplify
    "TCH", # flake8-type-checking
    "RUF", # ruff-specific
]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
check_untyped_defs = true

[[tool.mypy.overrides]]
module = "pymilvus.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "boto3.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "botocore.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "kfp.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "kubernetes.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "tabulate.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "matplotlib.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "numpy.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "ogx_client.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "openai.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "psycopg.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
